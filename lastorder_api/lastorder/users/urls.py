from django.urls import path
from views import UserViewSet

# path(r"", UserViewSet.as_view({'get': 'list', 'post', }), name='users' )
#
# path(r"<str:storeCode>", ItemViewSet.as_view({'get': 'list', 'post': 'create'}), name='items'),  # 상품 리스트
# path(r"dashboard/get/", DashboardView.as_view(), name='dashboard-get'),
# path(r"dashboard/set/", DashboardSetView.as_view(), name='dashboard-set'),

